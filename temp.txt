The <span class=\"variable-text-block-span-1\">prop</span> variable returns the names of any selected property groups of a layer that were on the selected layer in the timeline panel or the effects control panel. Selecting multiple groups on other layers will not return all selected groups on all selected layers, but only for the last selected layer.